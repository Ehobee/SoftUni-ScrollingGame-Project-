function initState() {
    const state = {
        player: "Ehobee",
        splatoon: {
            width: 130,
            height: 153,
            startX: 200,
            startY:200
        }
    };

    return state;
}